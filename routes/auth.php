<?php

// This file previously contained web/session-based auth routes (Breeze stubs).
// This project is API-only and uses Sanctum Bearer tokens. The web auth
// routes were intentionally removed to avoid confusion. Keep API routes in
// `routes/api.php` and protect them with `auth:sanctum`.

// If you need to re-enable session-based routes in the future, recreate
// the routes or restore from version control.
